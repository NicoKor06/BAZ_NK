meta {
  name: Login - get token
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "maxmustermann",
    "password": "geheim123"
  }
}
